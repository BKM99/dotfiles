# this script is used to install the things I need to get nvim working on ubuntu

# install nvim
sudo add-apt-repository ppa:neovim-ppa/unstable
sudo apt-get update
sudo apt-get install neovim

# install nvim plugin deps

# install node via nvm or fnm? need version greater than 14
# fd rg
# python pyenv?
# gcc g++ unzip
# wget curl gzip tar
# treesitter git cc
# make
# yarn

# install nerdfont
